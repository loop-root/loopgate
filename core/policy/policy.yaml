version: 0.1.0

# Loopgate starter policy
#
# This checked-in signed policy is intentionally conservative for a fresh public
# checkout:
# - Claude read-oriented hooks are allowed within the repo root
# - Claude writes and edits require approval
# - ambient shell, HTTP, and governed MCP stay disabled until an operator opts in
# - policy and persona mutation remain denied
#
# If you want a lower-friction daily engineering starting point, render a
# reviewed template first:
#   go run ./cmd/loopgate-policy-admin render-template -preset balanced
#
# Then sign and apply that reviewed policy through the normal signed-policy
# workflow.

tools:

  claude_code:
    deny_unknown_tools: true
    tool_policies:
      Bash:
        enabled: false
      Read:
        enabled: true
        allowed_roots:
          - "."
        denied_paths:
          - ".git"
          - "persona"
          - "runtime/state"
          - "core/policy"
      Glob:
        enabled: true
        allowed_roots:
          - "."
        denied_paths:
          - ".git"
          - "persona"
          - "runtime/state"
          - "core/policy"
      Grep:
        enabled: true
        allowed_roots:
          - "."
        denied_paths:
          - ".git"
          - "persona"
          - "runtime/state"
          - "core/policy"
      Write:
        enabled: true
        requires_approval: true
        allowed_roots:
          - "."
        denied_paths:
          - ".git"
          - "persona"
          - "core/policy"
          - "runtime/state"
          - ".claude/settings.json"
      Edit:
        enabled: true
        requires_approval: true
        allowed_roots:
          - "."
        denied_paths:
          - ".git"
          - "persona"
          - "core/policy"
          - "runtime/state"
          - ".claude/settings.json"
      MultiEdit:
        enabled: true
        requires_approval: true
        allowed_roots:
          - "."
        denied_paths:
          - ".git"
          - "persona"
          - "core/policy"
          - "runtime/state"
          - ".claude/settings.json"
      WebFetch:
        enabled: false
      WebSearch:
        enabled: false

  mcp_gateway:
    deny_unknown_servers: true
    servers: {}

  filesystem:
    allowed_roots:
      - "."
    denied_paths:
      - ".git"
      - "core/policy"
      - "persona"
      - "runtime/state"
      - ".claude/settings.json"
    read_enabled: true
    write_enabled: true
    write_requires_approval: true

  http:
    enabled: false
    allowed_domains: []
    requires_approval: true
    timeout_seconds: 10

  shell:
    enabled: false
    allowed_commands: []
    requires_approval: true

logging:
  log_commands: true
  log_tool_calls: true
  audit_detail:
    hook_projection_level: full

safety:
  allow_persona_modification: false
  allow_policy_modification: false
